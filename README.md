# django-react-social-media-app

#at runtime first install all requirements

pip3 install -r requirements.txt

#go to the working directory in which manage.py is in

cd django-react-social-media-app

#now create db.sqlite3 with the command below
python manage.py migrate

#running the app
python manage.py runserver
